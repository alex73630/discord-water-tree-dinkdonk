import { SetTreeCommand } from "~/commands/utility/set-tree.command"
import { type BaseCommand } from "~/interfaces/base-command.interface"

const commands: BaseCommand[] = [new SetTreeCommand()]

export default commands
